# Shazia Kausar — `shazia-k`

GitHub `shazia-k` · Slack `@shaziakausarwork` (`U0ABCMECSCR`) · server account `shazia`

## Identity — for her agent

- **The user is Shazia, not Frank.** GitHub work runs under her own `gh` login (`shazia-k`);
  issues and PRs she opens are assigned to her; she is never her own reviewer.
- Slack drafts are written for her to send, in her voice, and never @-mention her as a
  reviewer of her own work. Her usual channels: `#oriso-codereview`, `#oriso-dev-team`,
  `#oriso-daily-standup`.
- Her own UserService PRs: request `Shirloin` plus one more from the core list (the
  UserService pool minus her is only one person) — never Shanzae for UserService.
- She does not deploy to Pre-Dev; that is Frank's environment.

## Repositories

Lead on **ORISO-UserService**. Also works in ORISO-Frontend and ORISO-Admin.

## Review role

Lead reviewer for UserService. Reviews Helm and Docs. Shanzae is deliberately not in the
UserService reviewer pool — do not add her there.

## Notes for an agent

- Checkout lives under `~/Repos/ORISO/`, not `~/ORISO/`. Resolve paths from
  `~/.config/agent-routing/paths.env`; if that file does not exist, the kit installer writes
  a default one — do not guess a path and do not assume Frank's layout.
- Writes skills of her own. Finished skills go into the shared library at
  `/srv/dreambau/agent-skills/custom/` on `dreambau.com`; drafts and personal variants belong
  in `~/.oriso-dev-kit/local/skills/` or `/srv/dreambau/agent-skills/devs/shazia/`.
- Author of the `oriso-board-triage` skill (board claim → branch reality). Use it before
  trusting a board column.
