.git
node_modules
dist
# `tests` is intentionally not ignored: the verify stage copies it. The shipped
# runtime image never does, so this only widens the build context.
playwright-report
test-results
.env*
*.sqlite*
accounts.json
testmails.md
