apiVersion: v1
kind: Secret
metadata:
  name: email-proxy-secret
  namespace: wcr
  labels:
    app: email-proxy
    component: smtp-bridge
type: Opaque
stringData:
  # SMTP Configuration - Titan Email
  SMTP_HOST: "smtp.titan.email"
  SMTP_PORT: "587"
  SMTP_SECURE: "false"
  SMTP_USER: "verify@wcrbusiness.online"
  SMTP_PASSWORD: "h%=3-Ak]N;0<K6]"
  SMTP_FROM: "Cap <verify@wcrbusiness.online>"
  
  # Server port
  PORT: "3001"

