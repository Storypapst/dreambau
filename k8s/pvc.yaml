apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: testmails-data
  namespace: wcr
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
