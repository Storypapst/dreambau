apiVersion: v1
kind: Service
metadata:
  name: dreambau-evidence
  namespace: wcr
  labels: { app.kubernetes.io/name: dreambau-evidence }
spec:
  selector: { app.kubernetes.io/name: dreambau-evidence }
  ports:
    - { name: http, port: 80, targetPort: http }
