# Dockerfile for Webstudio Published Sites
FROM node:20-slim

WORKDIR /app

# Copy built application
COPY build ./build
COPY public ./public
COPY package.json ./
COPY package-lock.json* ./

# Install production dependencies only
RUN npm install --production

# Expose port
EXPOSE 3000

# Start the application
CMD ["npm", "run", "start"]





